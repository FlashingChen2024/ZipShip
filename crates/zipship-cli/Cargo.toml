[package]
name = "zipship-cli"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4", features = ["derive"] }
serde_json = "1"
time = { version = "0.3", features = ["formatting"] }
zipship-core = { path = "../zipship-core" }

